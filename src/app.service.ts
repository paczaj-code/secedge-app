import { Injectable } from '@nestjs/common';

/**
 * AppService is a service class that can be injected into other parts of the application.
 * This service can serve as a central point to define and manage the application's
 * core functionalities or business logic.
 */
@Injectable()
export class AppService {}
